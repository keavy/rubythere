- @page_title = "Account"

%h1 Account

- unless @events.blank?
  %h4 Your events
  %ul.events
  - @events.each do |event|
    %li= raw "#{link_to event.name, event_happenings_path(event)}"
- else
  %p You don't have any events yet to manage.
  %p= raw "#{link_to 'Add a new event?', new_event_path}"