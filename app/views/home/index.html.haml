- cache "events/attend" do
  = render :partial => 'events/attend', :locals => {:happenings => Happening.approved.upcoming}


= content_for :side do
  = render :partial => 'shared/box_search'
  = render :partial => 'shared/box_submit'
  = render :partial => 'shared/calendar'